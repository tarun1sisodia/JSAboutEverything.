
let loggedIn;
let token;

alert = prompt('Logged IN', loggedIn);
alert = prompt('Token', token);
let auth = loggedIn && token ? "Allow" : "Deny";
alert(loggedIn && token ? "Allow" : "Deny")

// console.log(auth);
